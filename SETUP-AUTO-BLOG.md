# 🚀 Налаштування автоматичної генерації блогів

## Крок 1: Встановіть залежності

```bash
npm install
```

Це встановить:
- `openai` - для генерації контенту через OpenAI API
- `ts-node` - для запуску TypeScript скриптів

## Крок 2: Додайте OpenAI API ключ в GitHub Secrets

1. Відкрийте ваш репозиторій на GitHub
2. Перейдіть: **Settings** → **Secrets and variables** → **Actions**
3. Натисніть **"New repository secret"**
4. Заповніть:
   - **Name**: `OPENAI_API_KEY`
   - **Secret**: ваш OpenAI API ключ (починається з `sk-proj-...`)
5. Натисніть **"Add secret"**

## Крок 3: Увімкніть GitHub Actions

1. Перейдіть: **Settings** → **Actions** → **General**
2. У розділі "Actions permissions" виберіть:
   - ✅ **"Allow all actions and reusable workflows"**
3. У розділі "Workflow permissions" виберіть:
   - ✅ **"Read and write permissions"**
   - ✅ **"Allow GitHub Actions to create and approve pull requests"**
4. Збережіть зміни

## Крок 4: Перевірте, що все працює

### Тест локально (опціонально):

```bash
# Додайте ключ в .env.local
echo "OPENAI_API_KEY=ваш-ключ-тут" >> .env.local

# Запустіть генерацію
npm run generate-blog
```

### Тест через GitHub Actions:

1. Перейдіть: **Actions** → **Generate Blog Posts**
2. Натисніть **"Run workflow"** → **"Run workflow"**
3. Почекайте 2-3 хвилини
4. Перевірте результат в логах

## 📅 Автоматичний розклад

Після налаштування статті будуть генеруватися автоматично:

- **Понеділок** о 12:00 (Київ)
- **Четвер** о 12:00 (Київ)

## ✅ Що відбувається після генерації?

1. GitHub Actions запускає скрипт
2. OpenAI генерує нову статтю (2-3 хвилини)
3. Створюється новий файл в `app/blog/[slug]/page.tsx`
4. Оновлюється `app/blog/page.tsx` (список статей)
5. Оновлюється `app/sitemap.ts` (для SEO)
6. Зміни автоматично комітяться в GitHub
7. Vercel автоматично деплоїть оновлення (2-3 хвилини)
8. Нова стаття з'являється на сайті!

## 💰 Вартість

- Кожна стаття коштує ~$0.10-0.30 (залежить від моделі GPT-4)
- 2 статті на тиждень = ~$0.40/тиждень
- ~$20/рік за 100+ статей

## 🎯 Теми статей

Система автоматично вибирає з 15+ актуальних тем про ФОП:

✅ Робота з іноземними клієнтами
✅ Податкові канікули
✅ Кредитування
✅ Страхування
✅ Найм працівників
✅ Електронний підпис
✅ Патентна система
✅ Облік доходів
✅ Франшизи
✅ Аліменти
✅ Онлайн-каси
✅ Субсидії
✅ Криптовалюта
✅ Інтернет-магазини
✅ Бухгалтерські програми

## 🔧 Налаштування

### Змінити розклад генерації

Відредагуйте `.github/workflows/generate-blog-posts.yml`:

```yaml
schedule:
  # Понеділок та четвер о 10:00 UTC (12:00 Київ)
  - cron: '0 10 * * 1,4'
```

Приклади:
- Щодня о 12:00: `'0 10 * * *'`
- Тричі на тиждень (Пн, Ср, Пт): `'0 10 * * 1,3,5'`
- Раз на тиждень (Понеділок): `'0 10 * * 1'`

### Додати нові теми

Відредагуйте `scripts/generate-blog-post.ts`, знайдіть `blogTopics` та додайте:

```typescript
{
  title: "Ваша нова тема",
  keywords: ["ключове слово 1", "ключове слово 2"],
  category: "Категорія"
}
```

### Змінити модель AI

У `scripts/generate-blog-post.ts` змініть:

```typescript
model: "gpt-4-turbo-preview"  // Якісно, але дорого
// або
model: "gpt-3.5-turbo"  // Дешевше, але менш якісно
```

## 🐛 Вирішення проблем

### ❌ Помилка: "API key is invalid"

**Рішення:**
1. Перевірте, що ключ правильно скопійовано
2. Перевірте, що ключ активний на platform.openai.com
3. Перевірте баланс на OpenAI акаунті

### ❌ Помилка: "Insufficient quota"

**Рішення:**
1. Поповніть баланс на platform.openai.com
2. Або змініть модель на дешевшу (gpt-3.5-turbo)

### ❌ GitHub Actions не запускається

**Рішення:**
1. Перевірте, що Actions увімкнено в Settings
2. Перевірте, що бот має права на запис (Write permissions)
3. Перевірте синтаксис YAML файлу

### ❌ Стаття згенерувалась, але не з'явилась на сайті

**Рішення:**
1. Перевірте, що зміни закомічені в GitHub
2. Перевірте статус деплою на Vercel
3. Почистіть кеш браузера (Ctrl+Shift+R)

## 📊 Моніторинг

### Перевірити останні генерації:

1. GitHub → **Actions**
2. Виберіть **"Generate Blog Posts"**
3. Перегляньте історію запусків

### Перевірити статус деплою:

1. Vercel Dashboard
2. Виберіть проект **fop-help**
3. Перегляньте останні деплої

## 🎉 Готово!

Тепер ваш блог буде автоматично поповнюватися новими SEO-оптимізованими статтями!

**Очікувані результати:**
- 📈 Зростання органічного трафіку
- 🎯 Більше ключових слів в Google
- 👥 Залучення нової аудиторії
- 🤖 Повна автоматизація контенту
